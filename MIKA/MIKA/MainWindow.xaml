<Window x:Class="MIKA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" Icon="mika.ico">
    <StackPanel>
        <Image Height="94" Source="title.png" />
        <Button Height="22" Width="200" Content="{Binding Path=RunAllText}" Command="{Binding Path=RunTest}"/>
        <ScrollViewer Height="426" VerticalScrollBarVisibility="Auto">
            <Grid>
                <ItemsControl ItemsSource="{Binding Path=Tests}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Width="200" VerticalAlignment="Center" Content="{Binding Path=Name}" Command="{Binding Path=RunTest}"/>
                                <Image Source="{Binding Path=ImageSource}"/>
                                <TextBlock Text="{Binding Path=Status}" 
                                           Foreground="{Binding Path=TextColor}" 
                                           FontWeight="UltraBold" 
                                           FontSize="26" 
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </StackPanel>
</Window>
